import React from 'react';
import './calculator.css';

const Quote = () => (
  <p className="quote">I can count all the lovers I have had on one hand...if I am holding a calculator.</p>
);

export default Quote;
